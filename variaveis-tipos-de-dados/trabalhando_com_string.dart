void main(List<String> args) {
  String nomeCompleto = 'Ariel Sardinha';

  print(nomeCompleto.length);
  print(nomeCompleto.isEmpty);
  print(nomeCompleto.isNotEmpty);
  print(nomeCompleto);
  print(nomeCompleto.toUpperCase());
  print(nomeCompleto.toLowerCase());
  print(nomeCompleto.replaceAll('a', 'p'));
}
