void main(List<String> args) {
  const aprovados = ['Ariel', 'Daniel', 'Carlos'];
  aprovados[1] = 'Novo valor';
  print(aprovados);

  // print(aprovados);
  // print(aprovados.elementAt(0));
  // print(aprovados[2]);
  // print(aprovados.length);
  // aprovados[0] = 'Pedro';
  // print(aprovados);

  // aprovados = ['Diego', 'Sabrina'];
  // print(aprovados);
}
